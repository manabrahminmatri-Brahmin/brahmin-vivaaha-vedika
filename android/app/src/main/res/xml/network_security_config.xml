<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <!-- 2factor.in SMS API - still uses HTTP for OTP delivery -->
        <domain includeSubdomains="true">2factor.in</domain>
    </domain-config>
</network-security-config>

# Release keystore configuration
# 1. Generate a production keystore:
#    keytool -genkey -v -keystore mana-vivaaha-vedika.keystore -alias mana-vivaaha-vedika -keyalg RSA -keysize 2048 -validity 10000
# 2. Place keystore in android/app/ directory (or secure location)
# 3. Fill in the values below (NEVER commit this file with real values)
# 4. This file is gitignored - store credentials in your CI/CD or password manager

storeFile=mana-vivaaha-vedika.keystore
storePassword=YOUR_STORE_PASSWORD
keyAlias=mana-vivaaha-vedika
keyPassword=YOUR_KEY_PASSWORD
